__pycache__/
*.py[cod]
*.so
*.o
*.egg-info/
.pytest_cache/
*.log

# JIT-compiled CUDA extension
src/_kernels/_build/

# measurement output -- bulk data is not vendored, see raw_data/README.md
*.jsonl
*.png
# ...except the planner sweep the paper's pipeline builds from: CPU-only,
# seeded and small, so it reproduces exactly and `make verify` works on a
# fresh clone with no GPU.
!raw_data/plan_sweep/**

# folders
calibration

# slide-layout previews: QA output for the deck, regenerate with
# `cd paper/presentations && python3 preview_deck.py`
paper/presentations/preview/

# secrets
*/*.env
