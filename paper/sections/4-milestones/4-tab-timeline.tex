% Table (tab:timeline). Body: _generated/timeline-table.tex, written by
% pipeline/tables/gen_table_timeline.py.
%
% The schedule itself is data in that script, not in this file. The float, the
% caption and the label are hand-written here, and the caption quotes the
% table's own macros so it cannot disagree with the rows underneath it.
% table*, not table: the milestone column holds sentences, and they need the
% full text width to wrap into something readable.
\begin{table*}[!t]
\centering
\caption{\capheadline{Milestones, one per week.} \timelineWeeksNum{} weeks from
proposal to final report. Calibration is front-loaded, so that every
measurement taken later is read through a cost model that has already been
validated against the evaluation hardware.}
\label{tab:timeline}
\input{sections/4-milestones/_generated/timeline-table.tex}%
\end{table*}
